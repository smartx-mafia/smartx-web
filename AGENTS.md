# SmartX 官网 · Agent 共同规则

## 必读与范围

- 当前唯一开发主线是 V4，公开首页为 `/`；`/v4` 只作旧链接兼容跳转。实施前必须完整阅读 `docs/website-v4.md`。
- `docs/website-v4.md` 是 V4 的唯一权威方案；`website-v4-design-brief.md` 和 V2/V3 文档只作历史参考。
- 每轮只修改当前指定的屏幕及必需的交接。已签字或已冻结的屏幕不得顺手重做。

## 设计决策顺序

事实准确、产品状态真实、基本可用性、性能与无障碍是硬底线。底线通过后，设计取舍按以下顺序决策：

1. **品牌辨识度与高级感**：不为展示技术、组件数量或动效数量牺牲整体气质。
2. **产品可信度**：真实 UI、真实字段和真实状态优先于抽象功能说明。
3. **叙事清晰度**：每屏只有一个主要判断和一个视觉主角。
4. **动效新奇度**：动效只在能解释状态变化和空间连续性时才存在。

## 禁止 Demo 感

- 禁止为了“展示能力”增加无意义的轮播点、切换器、浮动控制台或交互玩具。
- 禁止使用通用渐变卡片、玻璃拟态、随机光晕、无语义粒子和整页复用同一套 fade-up。
- 产品 UI 必须来自真实结构与设计系统；不绘制“看起来像产品”的占位 demo。
- 不用等权卡片墙代替构图。默认使用层级、留白、比例和精确对齐建立高级感。
- 动效必须有可追踪的起点和终点；上一屏的实体应能解释下一屏从哪里来。
- 阅读窗口内画面基本静止；运动集中在入场、交接和状态变化。

## 当前实施策略

- 先完成并验收桌面端；移动端在桌面端整体成立后单独编排。
- 移动端不得直接缩放桌面动效；但桌面实现不应制造无法降级的内容耦合。
- 每完成一屏，先以 1440×900 真实浏览器截图和滚动录屏验收，再进入下一屏。
- 代码交付前必须通过 typecheck、lint，并提供 reduced-motion 降级。
